name: atv_flutter_02
description: Atividades Flutter 02

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: "^0.10.0"
  flutter_app_name: ^0.1.0

flutter:
  uses-material-design: true

  assets:
    - lib/ui/assets/

flutter_app_name:
  name: "My Contact List"
